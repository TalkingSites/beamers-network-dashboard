export default function RetainersPage() {
  return (
    <div>
      <h1 style={{ fontFamily: "Georgia, serif", fontSize: "1.6rem", fontWeight: 700, marginBottom: "0.4rem" }}>
        Retainers
      </h1>
      <p style={{ color: "var(--text-secondary)", fontSize: "0.95rem" }}>
        Coming in Phase 4 — retainer overview.
      </p>
    </div>
  );
}
